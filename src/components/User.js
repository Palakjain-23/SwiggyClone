const User = (props) => {
    return(
        <div>
        <h1>Name :{props.name}</h1>
        <h2>location :{props.location}</h2>
        <h3>Hobby : devlopment</h3>
        </div>
    )
}
export default User;
